name: File
on:
  workflow_dispatch:
    inputs:
      cookies:
        description: Cookies
      file-url:
        description: File URL
        required: true

jobs:
  file:
    runs-on: ubuntu-latest
    steps:
      - name: Download file
        run: printf "%s\n" "FILENAME=$(curl -sSLOJ -w "%{filename_effective}" -b "${{ inputs.cookies }}" "${{ inputs.file-url }}")" >> "$GITHUB_ENV"

      - name: File
        run: |
          if [[ "$FILENAME" == *.zip ]]; then
            unzip -q "$FILENAME"
          elif [[ "$FILENAME" == *.apk ]]; then
            zip -q -d "lib/armeabi-v7a/*" "lib/x86/*" "lib/x86_64/*" && zip -q -d "META-INF/*"
          fi

          find . -type f -exec cksum -a sha256 {} +

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: output
          path: ${{ env.FILENAME }}
